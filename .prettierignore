node_modules
# playwright.config.ts suffixes this per suite (-vr, -api), matching the same
# variants .gitignore already lists.
playwright-report*
test-results
reports
allure-results
allure-report
vr-tests/**/*-snapshots

# VS Code parses this as JSONC; keeping it strict JSON avoids trailing commas.
agentic-playwright-suite.code-workspace

# Vendored from ella79/portfolio, kept byte for byte as reviewed upstream.
env/contract
