node_modules/
test-results/
playwright-report/
blob-report/
reports/
allure-results/
allure-report/
.auth/
*.log

# Baselines must match the CI rendering platform. Locally generated ones are
# for iteration only and are never the reference.
vr-tests/**/*-win32.png
vr-tests/**/*-darwin.png
.env
.DS_Store

# Reports generated locally for inspection, never committed.
allure-report-local/

# Yarn (node-modules linker, global cache: nothing but local state lands here)
.yarn/*
!.yarn/patches
.pnp.*
